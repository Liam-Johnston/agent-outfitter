This fragment exists to prove that instruction merging works inside a fresh
container: it should appear in the harness's instruction file wrapped in an
`agent-outfitter` managed region, with any hand-written prose in that file left
untouched.

smoke-test marker
